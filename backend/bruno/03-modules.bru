meta {
  name: List mounted modules
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v1/modules
  body: none
  auth: none
}

tests {
  test("200 and backtest module mounted", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().modules).to.include("backtest");
  });
}
